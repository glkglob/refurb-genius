/**
 * Projects domain types — pure, no infrastructure.
 * Authoritative definitions live in @repo/types; this module is the app-facing domain surface.
 */
export type {
  Project,
  NewProjectInput,
  ProjectStatus,
  ProjectStage,
  PropertyType,
  UKRegion,
} from "@repo/types";
