"use client";

// Re-export from @repo/ui (single source of truth after UI migration of command).
// All app imports via "@/components/ui/command" continue to work unchanged.
export * from "@repo/ui/command";
