fastapi>=0.115.0
uvicorn[standard]>=0.30.0
supabase>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
# NOTE: crewai is temporarily commented out because most versions do not yet
# support Python 3.14. The current backend code does not import crewai at runtime.
#
# When you want to use real CrewAI, switch to Python 3.11 or 3.12 and uncomment:
# crewai>=0.100.0
# crewai-tools>=0.12.0  # if needed

# Supporting libraries for FastAPI + Supabase + future CrewAI tool wiring
pydantic>=2.0.0
httpx>=0.27.0
python-multipart>=0.0.9
